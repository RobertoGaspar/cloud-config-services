server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/RobertoGaspar/cloud-config-services.git
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
          search-paths: config-data
  security:
    user:
      name: root
      password: s3cr3t

